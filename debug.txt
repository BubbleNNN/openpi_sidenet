GIT_LFS_SKIP_SMUDGE=1 uv pip install -e .
warning: The `tool.uv.dev-dependencies` field (used in `packages/openpi-client/pyproject.toml`) is deprecated and will be removed in a future re
lease; use `dependency-groups.dev` instead                                                                                                      Using Python 3.14.3 environment at: /home/mxuan.zhao/miniconda3/envs/openpi_sidenet
  × No solution found when resolving dependencies:
  ╰─▶ Because torch==2.7.1 has no wheels with a matching Python ABI tag (e.g., `cp314`) and openpi==0.1.0 depends on torch==2.7.1, we can
      conclude that openpi==0.1.0 cannot be used.
      And because only openpi==0.1.0 is available and you require openpi, we can conclude that your requirements are unsatisfiable.

      hint: You require CPython 3.14 (`cp314`), but we only found wheels for `torch` (v2.7.1) with the following Python ABI tags: `cp39`,
      `cp310`, `cp311`, `cp312`, `cp313`, `cp313t`
